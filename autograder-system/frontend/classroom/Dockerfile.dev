# 開発モード用Dockerfile
FROM node:18-alpine

WORKDIR /app

# 依存関係をインストール
COPY package*.json ./
RUN npm ci

# アプリケーションコードをコピー
COPY . .

# ポート3000を公開
EXPOSE 3000

# 環境変数
ENV PORT=3000
ENV HOSTNAME="0.0.0.0"

# 開発モードで起動
CMD ["npm", "run", "dev"]
